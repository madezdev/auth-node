# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/auth-node

# JWT Secret - Must be a strong secret in production
SECRET=your_jwt_secret_key_here

# Client URL for CORS (in production set to specific domain)
CLIENT_URL=http://localhost:3000
